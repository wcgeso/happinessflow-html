# Used only when running `vite --mode e2e` (see playwright.config.ts webServer
# and scripts/e2e/run.mjs). Points the client Firebase SDK at the local Auth +
# Firestore emulators instead of production.
VITE_USE_EMULATOR=true
